# HeyGen Account Credentials
# These credentials are used to automatically refresh the HeyGen session
# when cookies expire. The system will login to HeyGen using these credentials
# in the background to maintain an active session.

HEYGEN_EMAIL=your_heygen_email@example.com
HEYGEN_PASSWORD=your_heygen_password_here

# Production Deployment Settings
# Set NODE_ENV to 'production' for production deployments
NODE_ENV=development

# Render External URL (Render provides this automatically)
# This is used by auth-server to reload the browser context after login
# On Render: RENDER_EXTERNAL_URL=https://your-app.onrender.com
# Locally: Not needed (defaults to http://127.0.0.1:PORT)

# Port (Render will provide this automatically)
# PORT=3000

# Auth Secret (generate a secure random string for production)
AUTH_SECRET=dev-secret-change-me
